services:
  - type: web
    name: tabi-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: UNSPLASH_ACCESS_KEY
        sync: false
